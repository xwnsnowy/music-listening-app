import { Metadata } from "next";
import Artists from "@/app/(site)/artists/page";

export const metadata: Metadata = {
  title: "Artists",
  description: "Artists",
};
export default Artists;
